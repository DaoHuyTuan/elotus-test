import { Button } from "./ui/button"

export const LoginButton = () => {
    return (
        <Button
            // variant="ghost"
            className="text-base"
          >
            <span className="text-sm">Login</span>
          </Button>
    )
}